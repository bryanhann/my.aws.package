#!/usr/bin/env bash
THIS=$(python3 -c "from pathlib import Path;print(Path('$0').resolve())")
export MY_ROOT=$(dirname $(dirname $THIS))
SCRIPT=${MY_ROOT}/scripts/my-aws.py
uv python install 3.12
uv run \
    --python 3.12 \
    --with click \
    --with colorama \
    $SCRIPT $*
